/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "ibb.co",
      "example.com",
      "assets-netstorage.groww.in",
      "postimg.cc",
      "utfs.io",
    ],
  },
};

export default nextConfig;
